# 06. foundation_lab 设计说明

> 本节目标：把 `foundation_lab` 从“学习想法”收敛成真正的独立项目设计，明确项目目标、边界、结构、最小数据流和与 `04_rag`、`05_agent` 的关系，让你在动手前先把项目设计讲清楚

---

## 1. 概述

### 学习目标

- 理解为什么 `03_foundation` 需要一个独立项目而不是只有文档
- 理解 `foundation_lab` 的项目目标、能力边界和结构边界
- 理解为什么这个项目只做 `mock_retriever` 和 `mock_tool`
- 理解项目内三条最小数据流应该如何组织
- 理解 `foundation_lab` 和 `04_rag`、`05_agent` 的迁移关系

### 预计学习时间

- 项目目标与边界：0.5-1 小时
- 目录与结构设计：0.5 小时
- 数据流设计：0.5 小时
- 与后续阶段的关系：0.5 小时

### 本节在整体路径中的重要性

| 问题 | 本章解决方式 |
|------|-------------|
| `03` 只有概念没有项目 | 定义独立项目 `foundation_lab` |
| 写代码前边界不清 | 明确做什么、不做什么 |
| 结构容易失焦 | 提前固定项目骨架和数据流 |
| 和 `04/05` 容易混淆 | 明确迁移关系和阶段边界 |

> **这一章解决的核心问题不是“先写哪个文件”，而是“这个项目到底是什么、为什么存在、边界在哪里”。**

### 本章的学习边界

这一章重点解决：

1. `foundation_lab` 为什么必须存在
2. 这个项目到底负责验证什么
3. 项目结构和数据流为什么这样设计
4. 它和后续阶段如何衔接但保持独立

这一章不展开：

- 具体文件逐项实现细节
- 每天怎么排期
- 测试逐条怎么写

这些属于下一章 [07_foundation_lab_tasks.md](/Users/linruiqiang/work/ai_application/docs/03_foundation/07_foundation_lab_tasks.md)。

### 本章与前后章节的衔接

前面几章已经解决了：

- 为什么模型不能直接等于产品
- 为什么上下文组织影响效果
- 为什么方案选择不能乱来
- 为什么需要 LangChain 抽象
- 为什么需要最小工程骨架

这一章开始把这些理解收束成一个明确项目：

- `foundation_lab`

下一章则会把这个设计继续拆成可执行任务。

### 当前状态与第一入口

当前 `foundation_lab` 还没有正式开始编码，所以这一章的第一入口仍然是：

- 本文档本身

它当前承担的职责是：

- 在代码开始之前先把项目目标、边界、结构和最小数据流固定下来

未来正式编码时，这一章对应的项目承载目录应是：

- `source/03_foundation/foundation_lab/`

未来项目真正创建后，第一项目入口应是：

- `source/03_foundation/foundation_lab/README.md`

### 建议学习顺序

建议按这个顺序读：

1. 先读 `2-3`，固定项目为什么存在、做什么、不做什么
2. 再读 `4-5`，固定项目结构和三条最小数据流
3. 最后读 `6-7`，确认这个项目完成后应该获得什么，以及如何迁移到 `04`、`05`

卡住时回看：

1. `3. 项目目标和边界`
2. `4. 项目结构为什么这样设计`
3. `5. foundation_lab 的最小数据流`

### 项目映射

| 文档部分 | 对应代码/文档 | 角色 | 说明 |
|----------|---------------|------|------|
| 本章全文 | 本文档 | 第一阅读入口 | 当前先固定项目设计，不假设代码已存在 |
| `3. 项目目标和边界` | 未来项目 README、`qa_service.py` 的范围控制 | 项目边界说明 | 约束后续不越界实现 |
| `4. 项目结构为什么这样设计` | 未来 `app/`、`scripts/`、`tests/` | 计划目录骨架 | 固定模块职责 |
| `5. 最小数据流` | 未来 `qa_chain.py`、`mock_retriever.py`、`mock_tools.py`、`qa_service.py` | 计划主线能力 | 固定最小可验证路径 |
| `7. 和 04_rag、05_agent 的关系` | 后续阶段项目设计文档 | 阶段衔接 | 说明什么会迁移，什么不会提前做 |

---

## 2. 为什么 foundation_lab 必须存在 📌

### 2.1 如果没有这个项目会发生什么

如果 `03_foundation` 只有原理和抽象文档，没有独立项目，通常会出现两个问题：

1. 到 `04_rag` 时，LangChain 抽象还是“看过，但没真正写过”
2. 到 `05_agent` 时，Tool、Service、编排边界还是模糊的

这会让你在后续阶段反复出现一种状态：

- 概念知道，但代码下手不稳

### 2.2 为什么它不能直接等于 04 或 05

`foundation_lab` 的作用不是提前把 `04` 或 `05` 做掉。

如果你在 `03` 就直接做：

- 真正 RAG
- 真正 Agent
- 真正工作流

那你会在错误阶段引入过高复杂度。

所以这个项目的价值不在“做深”，而在：

- 把基础抽象和骨架做对

### 2.3 它在整个路线中的位置

更准确地说，`foundation_lab` 是：

- `02_llm` 之后的第一个真正组件化项目
- `04_rag` 和 `05_agent` 之前的骨架验证项目

它的角色类似于：

- 一个基础实验室
- 一个结构验证项目
- 一个工程认知过渡项目

---

## 3. 项目目标和边界 📌

### 3.1 项目目标

`foundation_lab` 的目标非常明确，只验证三件事：

1. 你能否把原生模型调用组织成最小服务
2. 你能否用 LangChain 抽象组织最小链路
3. 你能否分清 `Prompt / Chain / Retriever / Tool / Service` 的职责

注意，这里没有写：

- 真实检索效果优化
- Agent 智能性
- 工作流复杂度

因为这些都不属于本阶段任务。

### 3.2 做什么

`foundation_lab` 应该做：

- 最小问答
- 原生 SDK 与 LangChain 对照
- 最小 LCEL Chain
- `mock_retriever`
- `mock_tool`
- 最小 FastAPI 服务
- 最小日志和最小测试

### 3.3 不做什么

`foundation_lab` 不应该做：

- 真实向量数据库
- 真正 RAG 应用
- 真正 Agent loop
- 多步骤状态流
- 前端页面
- 部署平台化能力

这一点必须刻意控制，否则项目会快速失焦。

---

## 4. 项目结构为什么这样设计 📌

建议目录：

```plain
source/03_foundation/foundation_lab/
```

建议模块包括：

- `app/config.py`
- `app/llm/`
- `app/prompts/`
- `app/chains/`
- `app/retrievers/`
- `app/tools/`
- `app/services/`
- `app/observability/`
- `scripts/`
- `tests/`

这样设计的重点不在“标准答案”，而在于三个目的：

1. 职责清楚
2. 能跑起来
3. 后续可迁移

### 4.1 为什么要保留 native 和 langchain 两套调用

因为 `03` 阶段的关键学习目标之一，就是看清：

- 原生 SDK 让你更接近底层
- LangChain 让你更容易组织组件

如果只保留 LangChain：

- 你不知道它替你抽象了什么

如果只保留原生 SDK：

- 你很难建立后续阶段所需的统一组件心智

所以双轨并存，是刻意设计，不是重复劳动。

### 4.2 为什么 retriever 和 tool 都只做 mock

因为 `03` 的目标是理解抽象边界，而不是追求真实效果。

`mock_retriever` 的意义：

- 验证“知识入口”这个抽象

`mock_tool` 的意义：

- 验证“动作能力”这个抽象

它们的好处是：

- 简单
- 可控
- 易于对比

### 4.3 为什么 API / service / chain 必须分开

如果你只从目录上看：

- `main.py`
- `qa_service.py`
- `qa_chain.py`

你很容易产生一个错觉：

- 这只是把代码拆成了三个文件

但真正关键的不是“拆文件”，而是先把三层职责讲清楚。

更准确地说：

- `main.py` 是 API 适配层
- `qa_service.py` 是业务编排层
- `qa_chain.py` 是最小模型调用链

它们不是平行关系，而是有明确顺序的。

### 4.4 三层职责应该如何理解

`main.py` 负责：

- 接收 HTTP 请求
- 提取参数
- 调用 service
- 返回 HTTP 响应

`qa_service.py` 负责：

- 接收问题
- 判断当前应该走哪条路径
- 组织 retriever、tool、chain 的调用顺序

`qa_chain.py` 负责：

- 把输入组织成 Prompt
- 调用底层客户端
- 对输出做最小解析

这三层里，真正不应该做业务编排的是：

- `main.py`

真正不应该决定“是否检索、是否调用工具”的是：

- `qa_chain.py`

真正应该承担统一编排职责的是：

- `qa_service.py`

### 4.5 为什么先把这三层写进文档，而不是等代码自己长出来

因为如果文档里只写：

- 有 API
- 有 service
- 有 chain

但没有写清楚三层之间的调用顺序，后面代码几乎一定会自然滑向下面这种结构：

- 接口里直接拼 Prompt
- 接口里直接判断是否调用 retriever
- 接口里直接判断是否调用工具
- service 变成一层空转发

所以这部分必须先在文档里固定成项目约束：

```plain
request -> main.py -> qa_service.py -> qa_chain.py -> response
```

只有这样，代码才是在落实设计，而不是反过来替设计做决定。

---

## 5. foundation_lab 的最小数据流 📌

这一章最值得固定下来的，是项目里真正应该存在的三条最小路径。

### 5.1 一次请求进入项目后的总流转

在项目设计层面，任何一个问题进入 `foundation_lab` 后，都应该先按下面这个顺序理解：

```plain
HTTP request / script input
  -> main.py 或 scripts/*
  -> qa_service.ask(...)
  -> 路径判断
  -> plain / retrieval / tool
  -> qa_chain.invoke(...)
  -> prompt -> llm -> parser
  -> AskResponse / output
```

这里最重要的是两件事：

1. 入口可以是 API，也可以是脚本
2. 但统一编排入口必须是 `qa_service.py`

如果这一点不固定，整个项目就会退化成：

- 每个入口各写一套流程

这样后面扩展到 `04`、`05` 时会非常痛苦。

### 5.2 普通问答

```plain
input
  -> qa_service.ask()
  -> select_path() = plain
  -> qa_chain.invoke(question)
  -> prompt -> llm -> parser
  -> answer
```

这是整个项目的最小核心链路，也是你从 `04_langchain_core_abstractions.md` 开始真正写代码时要先跑通的部分。

这条路径的项目意义是：

- 先证明没有任何额外上下文时，最小问答链已经成立

它验证的是：

- Prompt 是否独立
- chain 是否独立
- service 是否能统一调度 chain

### 5.3 检索增强问答

```plain
input
  -> qa_service.ask()
  -> select_path() = retrieval
  -> mock_retriever.retrieve(question)
  -> docs
  -> qa_chain.invoke(question, context_blocks=...)
  -> prompt -> llm -> parser
  -> answer
```

这条链的重点不是效果，而是让你明确：

- 检索能力如何进入上下文

这条路径最值得强调的设计结论是：

- retriever 不直接给最终答案
- retriever 先返回文档
- 文档再被 service 整理后送入 chain

也就是说，retriever 在项目里的角色是：

- 知识入口

而不是：

- 最终回答者

### 5.4 工具辅助问答

```plain
input
  -> qa_service.ask()
  -> select_path() = tool
  -> select_tool(question)
  -> mock_tool(...)
  -> result
  -> qa_chain.invoke(question, tool_result=...)
  -> prompt -> llm -> parser
  -> answer
```

这条链的重点不是 Agent 智能，而是让你明确：

- 工具返回的是动作结果，不是知识文档

这条路径最值得强调的设计结论是：

- tool 先执行动作
- 动作结果再回到模型上下文

也就是说，tool 在项目里的角色是：

- 动作能力入口

而不是：

- 另一种 retriever

### 5.5 为什么路径判断要放在 service，而不是别的地方

路径判断是这个项目设计里最容易被写歪的一步。

它必须放在 `service` 层，而不是：

- 放在 `main.py`
- 放在 `qa_chain.py`
- 放在脚本文件里

因为路径判断本质上是：

- 业务编排决策

而不是：

- 接口适配
- Prompt 组织
- 模型调用细节

在当前阶段，路径判断故意只做成：

- 手工规则
- 简单关键词判断

这样做的目的不是偷懒，而是先把“编排发生在 service 层”这个结构固定住。

### 5.6 为什么这里还不做真正 Agent

因为这一阶段只需要：

- 先用 `service` 层做简单路径判断

不要现在就做：

- 自动工具选择循环
- 多步计划执行
- 状态图编排

这些都属于 `05_agent`。

---

## 6. 项目完成后你真正应该获得什么 📌

完成 `foundation_lab` 后，你真正应该得到的不是：

- “我做了个聊天接口”

而是：

- 我能用工程语言解释 AI 应用组件如何协作
- 我能说清 `Retriever` 和 `Tool` 的边界
- 我能说明为什么 `service` 层必须独立
- 我能把这个结构迁移到后续项目

这才是 `03` 阶段最值钱的产出。

---

## 7. 和 04_rag、05_agent 的关系 📌

`foundation_lab` 和后续阶段是独立项目，但存在明确迁移关系。

### 7.1 到 04_rag 的迁移

会迁移：

- 目录分层习惯
- 配置管理方式
- Prompt 管理方式
- chain / service 分层方式

会替换：

- `mock_retriever` -> 真实检索链路

### 7.2 到 05_agent 的迁移

会迁移：

- 工具模块思维
- service 编排思维
- 组件化结构

会升级：

- 简单路径判断 -> 真正工作流 / Agent 编排

### 7.3 为什么它们仍然保持独立

因为你的目标不是做一个一路叠加的失控项目，而是：

- `03` 有自己的学习价值
- `04` 有自己的技术深度
- `05` 有自己的系统复杂度

这也是为什么它们应该是独立项目，即使在同一个仓库中管理。

---

## 8. 本章小结

这一章你真正应该带走的是：

1. `foundation_lab` 是 `03_foundation` 必须存在的独立项目
2. 它的目标是验证抽象和骨架，而不是提前做掉 RAG / Agent
3. 项目结构的重点是职责清楚、可运行、可迁移
4. 三条最小数据流是项目的核心设计基础
5. 这个项目做对了，后面的 `04_rag` 和 `05_agent` 会顺很多

## 9. 实施动作

当前这章的实施动作，不是立即写完整项目，而是先把未来项目的设计结论固定下来：

1. 锁定项目只验证哪些能力
2. 锁定项目明确不做哪些能力
3. 锁定目录结构和最小数据流
4. 锁定它与 `04_rag`、`05_agent` 的迁移边界

当前阶段的输入是：

1. `01-05` 的原理与工程抽象文档
2. 本章的项目边界说明

当前阶段的输出是：

1. 一份明确的 `foundation_lab` 项目定义
2. 一份未来代码目录和数据流蓝图
3. 一份不越界的阶段说明

卡住时优先回看：

1. `2. 为什么 foundation_lab 必须存在`
2. `3. 项目目标和边界`
3. `5. foundation_lab 的最小数据流`

## 10. 完成标准

### 理解层

- 能解释为什么 `03_foundation` 必须有一个独立项目
- 能解释 `foundation_lab` 当前只验证哪些能力
- 能解释为什么当前阶段不应该提前做真正 RAG / Agent

### 操作层

- 能按建议顺序读完本章重点
- 能根据本章说明明确项目范围
- 卡住时知道该回看哪几个关键小节

### 代码准备层

- 能说明未来项目目录应该如何组织
- 能说明三条最小数据流会分别落在哪些模块
- 能说明未来项目 README 应该承担什么职责

### 映射层

- 能说清本章与未来 `source/03_foundation/foundation_lab/` 的关系
- 能说清本章与 `07_foundation_lab_tasks.md` 的承接关系
- 能说清本章与 `04_rag`、`05_agent` 的迁移边界
