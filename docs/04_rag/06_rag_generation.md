# 06. RAG 生成

> 本节目标：把前五章的输入、向量、检索能力真正接成用户可见的问答链路，掌握 RAG Prompt、上下文格式化、来源引用和无答案处理。

---

## 1. 概述

### 学习目标

- 理解为什么检索结果还需要经过上下文组织和 Prompt 设计
- 能说明 RAG Prompt 和普通聊天 Prompt 的差异
- 理解为什么答案应该返回 `answer + sources`
- 理解为什么无答案场景必须显式处理
- 掌握固定 RAG Chain 的基本结构

### 预计学习时间

- RAG Prompt 与上下文组织：1 小时
- 来源引用与拒答：45 分钟
- 固定 RAG Chain 设计：1 小时

### 本节在 AI 应用中的重要性

| 场景 | 相关知识 |
|------|---------|
| 文档问答 | context formatter、RAG Prompt |
| 来源引用 | `answer + sources` |
| 无答案处理 | 阈值、拒答、边界控制 |
| 服务层实现 | Chain、Service、统一返回对象 |
| 效果诊断 | 检索问题 vs 生成问题的区分 |

> **第六章是“检索系统”第一次真正变成“问答系统”的地方。**

### 本章与前后章节的关系

第五章已经解决：

1. 如何召回更合适的上下文
2. 检索失败时先看哪里

第六章接着解决：

1. 如何把检索结果组织成模型可读上下文
2. 如何让模型基于上下文回答
3. 如何返回答案和来源

第七章会继续建立在这里之上：

1. 对问答链路建立最小评估闭环
2. 区分改动到底让什么变好了

### 本章的学习边界

本章重点解决：

1. context formatter
2. RAG Prompt
3. 固定 RAG Chain
4. answer + sources
5. 无答案处理

本章不系统展开：

- Agent loop
- 多工具动态决策
- 复杂状态机
- 前后端完整交互系统

### 当前实现状态

当前仓库里：

- [phase_6_rag_generation/README.md](/Users/linruiqiang/work/ai_application/source/04_rag/labs/phase_6_rag_generation/README.md) 仍是占位
- 第六章代码快照尚未落地

---

## 2. 为什么检索结果不能直接扔给模型 📌

### 2.1 检索结果本身还是程序对象

第五章拿到的通常还是结构化检索结果：

- 文本内容
- 分数
- metadata
- 来源身份

这还不是模型天然最适合消费的输入形式。

模型更适合的通常是：

- 明确格式化过的上下文片段
- 明确的回答任务
- 明确的引用和拒答约束

### 2.2 RAG Prompt 和普通聊天 Prompt 的区别

普通聊天 Prompt 的重点是：

- 角色
- 任务
- 风格

RAG Prompt 还必须额外回答：

1. 上下文从哪里来
2. 是否必须优先依据上下文回答
3. 如果上下文没有依据该怎么处理
4. 来源要不要返回、怎么返回

所以第六章的核心不是“再写一个 Prompt”，而是：

> 让模型在受控上下文里回答，而不是自由发挥。

---

## 3. 第六章要解决的几个关键问题 📌

### 3.1 context formatter

context formatter 负责的是：

- 把检索结果拼成清晰文本
- 控制片段顺序
- 决定是否保留来源信息
- 控制上下文长度和展示方式

它不是简单字符串拼接，而是：

> 检索层和生成层之间的翻译器。

### 3.2 来源引用

很多业务场景里，最终返回值不能只有答案。

还需要：

- 引用来源
- 帮助用户复核
- 帮助后续调试

所以一个更稳的返回结构通常不是：

```text
"一段答案"
```

而是：

```text
answer + sources
```

### 3.3 无答案处理

RAG 系统不是“永远给个答案”。

真正稳的系统还要能处理：

- 没召回到依据
- 召回结果明显不相关
- 问题超出知识库范围

所以第六章必须把“拒答或保守回答”当成主线能力，而不是异常情况。

---

## 4. 固定 RAG Chain 的边界 📌

第六章默认做的是固定 RAG Chain：

```text
question
-> retriever
-> context formatter
-> prompt
-> llm
-> answer + sources
```

它的价值在于：

- 数据流稳定
- 容易调试
- 容易评估
- 便于区分检索问题和生成问题

这也是为什么本课程把复杂动态决策留到 Agent 阶段，而不是在这里提前混进来。

---

## 5. 第六章实现后应该怎么学

等第六章真实代码快照落地后，建议按这个顺序：

1. 先看 `context formatter`
2. 再看 Prompt 模板
3. 再看 Chain 组合
4. 最后看 Service 如何返回 `answer + sources`

本章最终完成标准应该是：

- 能跑通最小 RAG 问答链路
- 答案能携带来源
- 能处理无答案问题
- 能说明哪些问题是检索问题，哪些是生成问题

---

## 综合案例：为什么明明检索到了还答不好

```python
# 你发现一个问题：
# Retriever 已经召回了正确 chunk，
# 但最终答案还是很散，来源也不清楚。
#
# 请回答：
# 1. 这更像检索问题还是生成问题？
# 2. context formatter 可能应该检查什么？
# 3. RAG Prompt 里至少应加入哪些约束？
# 4. 为什么最终返回结构最好是 answer + sources，而不是单一字符串？
```
