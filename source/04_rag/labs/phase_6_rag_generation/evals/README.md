# evals

本目录用于存放 `rag_lab` 的评估数据和实验记录。

当前已经包含：

- `retrieval_bad_cases.json`
  用于第五章对比 similarity / threshold / MMR / filter 的最小坏案例集。

后续建议继续补入：

- `golden_set.jsonl`
- `experiments.md`
- `bad_cases.md`
