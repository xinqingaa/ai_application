# 需求评审助手产品规格

本文是需求评审助手“为什么做、为谁做、必须做什么”的唯一产品规格。领域对象、状态机、权限矩阵、证据规则、事务和接口等详细设计见 [SDD.md](SDD.md)；代码结构与实现顺序见 [PLAN.md](PLAN.md)。真实 Schema、OpenAPI、migration 和测试是精确执行真源，但必须符合本规格和 SDD 的稳定契约。

## 1. 产品定位

需求评审助手是以需求基线为核心的需求定义、评审与交付工作台。它接收结构化需求输入、已有 PRD 或对已有需求的变更，结合内部业务规则、历史资料和项目内已批准需求，把输入收敛为结构化、可追溯、经过逐项决策、可批准、可版本化的需求基线，并导出人和 Agent 都能读懂的需求交付包。

产品不是一次性的“PRD 评审报告生成器”。评审贯穿需求形成、收敛、批准和迭代全过程：每条需求主张保留形成来源，每条评审结论回到可定位依据；证据不足时拒绝强事实结论并转成用户可以回答的问题，但不拒绝由人明确做出的产品选择。

产品沿一条主线分两个连续阶段：

- 第一阶段用固定 RAG 建立需求定义、可信评审、人工批准、基线与交付闭环。
- 第二阶段在同一对象模型和批准门上增加 Agent、Tools、Deep Research 与 Multi-Agent，让 Agent 在人的裁决下追问、提出补丁并分析变更影响。

阶段不是并行产品版本，仓库只维护一个需求评审助手。

## 2. 用户、角色与核心任务

主要用户是需要定义、评审和交付需求的产品与研发人员，评审重点是规则冲突、遗漏、歧义以及 Web、Flutter、服务端影响。

产品区分两层角色：

- 系统级：`admin / member`。系统管理员维护全局知识资料，普通成员可以创建项目。
- 项目级：`owner / editor / viewer`。权限取系统身份与项目成员身份的交集；系统管理员不会自动获得项目批准权。

提交批准、退回或撤回、批准、正式导出、项目成员管理、编辑 Project Brief 六类动作只能由人触发。第二阶段 Agent 以发起者的项目角色行动，没有独立角色，也不能通过 Tool 权限绕过这些门。

用户需要完成的核心任务：

- 从固定表单、已有 PRD 或已批准基线形成结构化需求版本。
- 由 editor 或 owner 明确创建或选择一个 Requirement，作为后续固定流程或 Agent 运行唯一可写入的 draft 容器。
- 区分人写、导入、AI 建议、外部事实与旧版本派生等来源。
- 找出内部缺失与矛盾、外部规则冲突、项目内已批准需求冲突和研发影响。
- 在证据不足时得到可回答的补充问题，而不是无依据的强结论。
- 逐项处理评审结论，保留可审计 Decision 和条目级差异。
- 由人批准不可变需求版本成为 Requirement 基线，并从基线导出交付包。
- 从基线派生新版本继续迭代。
- 第二阶段在受治理工具、研究和多角色协作下推动复杂需求收敛与影响分析。

完整动作矩阵见 [SDD 第 6 节](SDD.md#6-身份权限与人工门禁)。

## 3. 输入与输出

### 输入

- 用户身份、系统角色与项目角色。
- 由项目 owner 维护的 Project Brief。
- 第一阶段的固定表单输入或导入 PRD；导入支持粘贴文本及 TXT、Markdown、DOCX、文本型 PDF。
- 人对待确认条目、Finding、批准流程和导出的显式动作。
- 系统管理员维护的现行业务资料、历史材料及其发布版本。
- 检索范围、资料角色和证据资格约束。
- 第二阶段的模糊想法、追问回答、会话上下文、工具权限、预算、人工确认和受控评审工作区。
- 用户明确确认保存的跨会话偏好及其查看、更新、删除和关闭请求。

第一阶段不接收模糊想法，也不使用动态 Agent 追问；这属于第二阶段。

模糊想法进入第二阶段前必须已有明确的 Project，且由 editor 或 owner 人工创建或选择目标 Requirement 的 `draft` 版本。Agent 可以建议标题、分区和内容，但不创建正式 Requirement；它只在这个 draft 上提出补丁，仍由人确认后写入。

### 输出

- 有固定分区、稳定条目身份、来源、确认状态和修订号的需求版本。
- 每个固定分区的处理状态：已覆盖、人工标记不适用并留理由，或仍待补充。
- PRD 导入覆盖、原文定位和未映射内容诊断。
- ReviewRun、可定位 Finding、Sources、Citation、证据充分性、Refusal 和补充问题。
- 经过最终校验的结果以及与生成中未校验增量的明确区分。
- 人的 Decision、按条目对齐的版本 Diff 和批准记录。
- Requirement 当前基线及已批准版本索引状态。
- 从不可变版本导出的 Markdown 与 JSON DeliveryPackage。
- 真实运行状态、停止原因、成本、延迟和依赖错误。
- 第二阶段的 Brief 草案、需求补丁 Diff、变更影响分析、Tool Call、研究证据和 Agent 协作结果。

## 4. 核心产品模型

产品主链为：

```text
Project → Requirement → RequirementVersion → RequirementItem
```

- Project 保存成员、Project Brief 和导入原文。
- Requirement 是一个可独立版本化、批准和交付的稳定需求主题，持有当前基线指针。
- RequirementVersion 是一次可评审、可批准的内容快照；批准后不可修改。
- RequirementItem 是需求正文的最小单位，通过稳定业务身份跨版本对齐。
- RequirementVersion 对每个固定分区保留可解释的处理状态；“有正文”与“该分区已被明确处理”不能混用。
- ReviewRun 对版本的一个确定修订执行评审并产生 Finding。
- Decision 记录人对 Finding、待确认条目或第二阶段 Agent 补丁的裁决。
- DeliveryPackage 从已批准或被替代的不可变版本导出。

基线只在 Requirement 粒度。全产品基线、Requirement 归档和项目自定义分区不是当前产品目标。完整字段、枚举、来源与状态规则见 [SDD 第 3–5 节](SDD.md#3-领域对象与稳定身份)。

### 4.1 完整需求与批准资格

“完整需求”不是模型生成看似完整的长文本，而是每个固定分区都被显式处理。除可选的 `other` 外，分区必须处于以下之一：

- **已覆盖**：存在至少一条已确认的对应条目。
- **不适用**：人明确标记并留下理由。
- **待补充**：当前没有足够的用户意图或事实，不能伪造默认值。

只有前两种状态可进入批准门；`待补充` 必须作为可定位缺口阻断批准。此规则补充而不替代 Finding、Citation、Decision 和人工批准：分区被覆盖不代表外部事实已获 Citation，Finding 被处理也不代表遗漏分区已自动补齐。

## 5. 第一阶段：固定 RAG 产品闭环

第一阶段必须形成可运行的固定 RAG 需求定义、评审与交付工作台：

```text
真实资料 → 解析与来源保留 → Chunk / Metadata
→ PostgreSQL FTS + pgvector → RRF → Retriever 诊断
→ Context → 结构化生成 → Citation 支持性与证据充分性
→ 结构化需求版本 → Finding → 人的 Decision
→ 提交 → 人工批准 → Requirement 基线
→ 事务后索引 → 人工导出 DeliveryPackage
```

第一阶段必须具备：

- 真实文档、真实 PostgreSQL、真实 Embedding 和真实模型主路径。
- 固定表单创建与导入 PRD 两个入口，汇入同一需求对象模型。
- 固定分区完成度的确定性检查；缺失分区产生可定位的补充需求，不能因模型未报告而通过批准。
- 来源、Citation 和 Decision 三分，产品意图不因缺 Citation 被拒绝，外部事实不能靠人的确认代替 Citation。
- 可定位 Finding、逐项 Decision、条目级 Diff 和证据不足时的 Refusal 或补充问题。
- RequirementVersion 的人工状态迁移、批准门、不可变决策快照、原子基线切换与事务后索引。
- 全局知识库与同项目已批准需求检索池两个独立证据来源。
- 用户身份、两层角色、知识资料上传—暂存—发布流程。
- Review API、结构化增量与最终校验替换的 SSE 契约，以及以需求正文为中心的 Web 工作台。
- 固定 Golden Set、直接 LLM / Lexical / Dense / RRF 对照，以及可见的成本、延迟和错误。

第一阶段不建设 Agent、Multi-Agent、通用 Workflow、完整质量平台、Flutter 产品端或通用知识运营平台。RequirementVersion 的持久状态由人的显式动作迁移；ReviewRun、资料解析和索引状态由固定程序推进，且不得自动改变需求版本状态。

## 6. 第二阶段：Agent、Tools 与 Multi-Agent

第二阶段在第一阶段对象模型与批准门上增加：

- 框架驱动的 Agent Harness、Tool Runtime、状态、停止、恢复、事件和观测。
- Agentic RAG、Query Rewrite、Source Routing 和受控补检索。
- 从模糊想法识别缺口并通过 Interrupt 追问，形成 Brief 草案与结构化需求草稿。
- editor 或 owner 先人工创建或选择目标 Requirement draft；Agent 只在该版本上追问和提出补丁，不创建正式 Requirement。
- `propose_requirement_patch → Diff → 人确认 → apply_requirement_patch` 的正式写入门；apply 后仍需重新评审。
- MCP、Search、Browser、File Read、运行级暂存写入和受控 Code Tool。
- 条目差异对规则、接口契约、客户端模型和定向测试的变更影响分析。
- Conversation、Run State、短期摘要和经用户确认的长期偏好记忆。
- Agent Skills、Deep Research、Multi-Agent 与真实 A2A 互操作。
- 必要的 Workflow、Checkpoint、恢复、人工介入和副作用治理。
- 固定 RAG、单 Agent 与 Multi-Agent 的质量、成本、延迟和失败定位比较。

Agent 只能形成 Brief 草案，不能写 Project Brief；正式需求只能经补丁 Diff 的人工确认写入草稿。File Write 不修改正式需求，也不创建 DeliveryPackage。完整工具、安全、记忆和写入边界见 [SDD 第 9–12 节](SDD.md#9-第二阶段-agent-写入与影响分析)。

## 7. 固定垂直场景

两个阶段复用“售后入口与订单状态”垂直切口：

- Project 是一个电商 App。
- 第一阶段把“售后入口”PRD 导入为 Requirement 的 v1，经评审和人工批准成为基线。
- 同一 Project 另有一个小型“订单状态展示”Requirement 的已批准、已索引基线，用于稳定验证项目检索池和已批准需求冲突。
- 第二阶段的“售后接口 v2 与多端契约一致性评审”是同一“售后入口”Requirement 的新版本，不是新 Requirement。

固定切口用于控制比较变量，不表示产品只能处理这一类需求。

## 8. 产品交互原则

- 主界面以需求正文和人的决策为中心，运行轨迹进入可展开详情。
- 用户能逐条区分来源、已验证事实、产品意图、推断、证据不足和真实系统失败。
- Finding 就近挂在条目、分区或版本上，Decision 在 Finding 旁完成。
- 生成中的未校验内容不能伪装成最终结论；最终校验失败时必须显式撤回。
- 权限在后端路由与动作层执行，前端隐藏按钮不是授权依据。
- 草稿只能生成带明确标记的非正式预览，正式 DeliveryPackage 只能由人从不可变版本导出。

## 9. 安全、证据与治理原则

- 模型提出候选内容或行动，应用负责校验、授权和执行。
- 每条外部事实和外部规则冲突必须回到合格 Citation；内部缺失与矛盾回到当前需求或 Project Brief 定位。
- 当前待评审版本和自身旧基线不能伪装成外部证据。
- 工具默认最小权限，读取、暂存写入、正式需求写入和外部行动严格分开。
- 文件、网页、Tool Result 和远程 Agent 输出都视为不可信输入，不能改变系统规则或权限。
- Code Tool 不提供任意 Shell，默认只读输入、隔离输出、禁网并限制资源。
- 长期记忆只保存用户明确确认的偏好；业务资料、模型推断和 Tool Result 不能自动写成偏好，偏好也不能成为 Citation。
- 缺少 Key、鉴权、限流、超时、能力不支持和协议失败必须真实暴露，不静默降级为 Mock、假向量或静态成功。

## 10. 非目标

- 通用项目管理或文档协作平台。
- 多个平行产品、通用 Agent 平台、工具市场或低代码画布。
- 可配置多级审批链、审批委托、通知平台和企业权限中台。
- 全产品基线、Requirement 归档、项目自定义分区和独立 Brief 版本界面。
- 默认接入 Reranker、GraphRAG、RAPTOR、OCR/VLM 或完整知识治理平台。
- 任意文件覆盖、任意 Shell、默认联网 Code Tool 或无治理的 MCP / A2A 能力。
- 用 Multi-Agent 数量证明效果，或为展示复杂度引入没有独立责任的 Agent。
- 完整评估平台、多租户和 Kubernetes 级平台设施。

## 11. 完成标准

最终产品必须：

- 可运行、可测试、可观察、可评估。
- 从输入、来源、Citation、Finding、Decision、批准基线到 DeliveryPackage 全链可回查。
- 在证据不足时拒绝强结论，并给出可回答的补充问题。
- 保证批准内容、批准 Brief 修订和批准决策集合不可变且可验证。
- 保证六类人工动作不能被 Agent、Tool 或系统管理员身份绕过。
- 对真实模型、数据库、Embedding、文件、协议和工具失败给出可定位错误。
- 只有固定分区均已覆盖或被人明确标为不适用、且满足既有 Finding、Citation、Decision 与批准门后，需求版本才能作为完整基线批准。
- 保留固定 RAG 基线，并对 Agent 和 Multi-Agent 的收益与代价形成同条件证据。
- 能修改一个业务规则、工具或 Agent 责任并完成回归。

第一阶段的详细确定性验收不变量见 [SDD 第 13 节](SDD.md#13-确定性验收不变量)，第二阶段的运行、工具、研究和协作验收见 [SDD 第 14 节](SDD.md#14-第二阶段验收契约)；项目篇把这些契约转化为学习检查点与验收证据。
